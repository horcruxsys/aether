[package]
name = "shield-wasm"
version = "0.1.0"
edition = "2024"
description = "Aether Edge Compute WASM bindings for zero-trust PII scrubbing"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.99"
shield = { version = "0.1.0", path = "../shield-pii" }
serde-wasm-bindgen = "0.6.5"
