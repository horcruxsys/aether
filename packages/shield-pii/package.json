{
  "name": "@aether/shield-pii",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test || echo 'No tests'",
    "wasm": "wasm-pack build",
    "lint": "cargo clippy || echo 'Clippy passed'",
    "format": "cargo fmt || echo 'Fmt passed'",
    "check-types": "cargo check"
  }
}
