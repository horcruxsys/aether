[project]
name = "aether-entity-resolution"
version = "0.1.0"
description = "Union-Find entity resolution engine for Aether Data Fabric"
authors = [
    { name = "Aether Contributors" }
]
dependencies = []
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aether_entity"]
