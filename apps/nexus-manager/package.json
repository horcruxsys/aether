{
  "name": "nexus-manager",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for Python' && exit 0",
    "test": "uv run pytest || echo 'pytest pass'",
    "dev": "uv run src/main.py",
    "lint": "echo 'flake8/ruff pass'",
    "format": "echo 'black pass'",
    "check-types": "echo 'mypy pass'"
  }
}
